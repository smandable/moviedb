DB_HOST=localhost:3306
DB_USERNAME=your_username
DB_PASSWORD=your_password
DB_DATABASE=movieLibrary
DB_TABLE=movies
UPDATE_MISSING_DATA_ONLY=false

# Scan/rename endpoints reject any path that resolves outside this base
# directory (guards against arbitrary filesystem access). Default: /Volumes.
# Set to "/" to disable the guard.
ALLOWED_BASE_PATH=/Volumes
